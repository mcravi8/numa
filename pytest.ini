[pytest]
# Put the repo root on sys.path so tests can `from main import app`.
pythonpath = .
testpaths = tests
markers =
    network: test requires live internet access (yfinance / Yahoo Finance); auto-skipped offline
